package com.pepej.gamma.job.types;

public enum JobType {
    PLAYER_MESSAGE,
    GAME_MESSAGE,
    TIMER_CREATE,
    TIMER_START,
    TIMER_TICK,
    TIMER_INTERRUPT,
    TIMER_STOP,
    GAME_EVENT;
}
